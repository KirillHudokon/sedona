/*
function PlusMinus(obj) {
    this.minus=document.querySelectorAll(obj.minus);
    this.plus=document.querySelectorAll(obj.plus);
    this.inputs=document.querySelectorAll(obj.inputs)
    var self=this;
    for(var i=0; i<this.plus.length;i++){
        this.plus[i].onclick=function () {

        };
    }
    function _plus() {
        return self.input[i].value++
    }
    function _minus() {
        self.inputs[self.i].value--
    }
}*/
